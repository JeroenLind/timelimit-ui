name: "Timelimit UI"
version: "0.1.31"
slug: "timelimit-ui"
description: "Minimal Home Assistant add-on skeleton met s6-overlay"
arch:
  - amd64
  - armv7
  - aarch64
startup: before
boot: auto
init: true
options:
  test_message: "Hello from Timelimit UI"
schema:
  test_message: "str"